[tox]
envlist = py26, py27, pypy, py33, py34, py35

[testenv]
passenv = NASS_API_KEY
deps =
    pytest
    pytest-cov
commands =
    py.test -cov=nass --cov-report=term-missing -vv

[testenv:coveralls]
deps = coveralls
commands =
    coverage combine
    coverage report
    coveralls
